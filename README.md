# Product Catalog

A small React + TypeScript product catalog practicing state, controlled inputs, conditional rendering, validation, and TypeScript.

## GitHub

[GitHub Repository](https://github.com/y-seyha/product-catalog)

The app runs from the `main` branch.

## Screenshots

* Product grid with stock badges
* In-stock filtered view
* Form validation error
* `npx tsc --noEmit` passing

## TypeScript

When `price` was deliberately passed as a string, `tsc` flagged a type mismatch because `price` is defined as a `number`.

